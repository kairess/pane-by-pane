# 인수인계 (2026-09-16 ~ 09-22 작업, 09-22 후속 포함)

다른 컴퓨터에서 이어서 작업하기 위한 메모입니다. 코드가 어떻게 동작하는지는 `README.ko.md`에 있고, 여기에는 이번에 무엇을 왜 바꿨는지, 어디까지 확인했는지, 무엇이 남았는지를 적습니다.

## 시작하기

```sh
git clone https://github.com/kairess/pane-by-pane.git && cd pane-by-pane
npm install
npm run typecheck && npm test      # tsc, node --test (43개)
npm run dev                        # http://localhost:5173
```

Node 22.18 이상이면 `.ts`를 그대로 실행합니다. 검증 스크립트는 `scripts/`에 있습니다(아래).

## 이번에 바뀐 것

### 1. 홈 화면 바로가기 (PWA manifest)
- `public/manifest.webmanifest`, `public/icons/`, `index.html`의 `<link rel="manifest">`·아이콘·`theme-color`.
- `start_url`·`scope`·`id`가 모두 `./`라서 퍼즐 해시(`#w=6&…`)가 붙은 상태에서 홈 화면에 추가해도 루트로 열리고, 루트는 마지막 창을 복원합니다.

### 2. 한 셀에 기호 하나 (`src/engine/generator/clues.ts`)
- 셀 단서(장미창 기호 → 폴리오미노 → 넓이의 수 순)가 같은 셀을 쓰지 않습니다.
- 대가: 직사각 판 + 고정 경계선 없음 + 넓이의 수 + 장미창은 기호 셀의 숫자가 빠져 유일해 검사가 자주 실패합니다(7×7 4초, 8×8 9초, 9×9 기호 4개는 실패). 앱 기본값(비정형 창 + 고정 경계선)에서는 문제없습니다. 장미창 기호 재배치 8회(`generate.ts`의 redeal 루프)로 완화했습니다.

### 3. 표식(쌍둥이·닮지 않은 둘) 글리프 (`src/web/board.ts`)
- 폰트 글리프 대신 선으로 그려 원 중심에 맞췄습니다. 폰트의 수학 축 때문에 위로 떠 보이던 문제.

### 4. 난이도 측정 재설계 (`src/engine/logical.ts`)
- **lookahead 기법(3등급)**: 반대로 놓으면 규칙 검사가 즉시 실패하는 엣지를 가정이 아닌 직접 추론으로 처리. 예전에는 가정의 37%가 이런 경우였습니다.
- **가정 등급을 내용으로**: 모순까지의 라운드 수가 아니라 가정 안에서 확정된 엣지 수(≤3 짧음, ≤10 연쇄, >10 깊음).
- **점수** = 최고 등급 + 가정 비용 합 + 0.2×직접 추론 사슬(격자 크기 보정). 별 경계 `[3.5, 5.5, 8, 10.5, 14, 18]`. 대략 3★=짧은 가정 하나, 5★=깊은 가정 하나, 7★=깊은 가정 둘 이상.
- 힌트 문구: `src/web/i18n.ts`의 `lookaheadWall/Join`, `technique.lookahead`.

### 5. 난이도를 "요청"으로 만드는 생성기 (`src/engine/generator/generate.ts`)
- 같은 정답으로 지우는 순서를 바꿔 최대 8번(`ordersPerSolution`) 최소화한 뒤 새 정답으로. 정답 하나가 순서에 따라 2~7★가 나오므로 여기가 분산의 원천입니다. 결과가 반복되면 즉시 새 정답으로.
- 목표 하한 3★ 이상이면 4개 묶음 제거를 건너뛰고 낱개로 지웁니다(무작위 낱개가 시간당 7★ 적중률이 가장 좋았음. 유도·전수 탐욕은 느려서 제외).
- 목표 미달이면 `missed: true`와 함께 가장 가까운 퍼즐을 돌려주고, 웹은 "요청한 난이도가 아닙니다" 대화상자, CLI는 표시를 붙입니다.
- 전체 단서로도 상한을 넘는 요청(1★ 등)은 상한을 그 값으로 올려 "가능한 한 쉬운" 창을 만들고 미달로 표시합니다.

### 6. 구역 넓이 설정 제거 → 자동 (`autoSizeBand`)
- 웹 입력란을 없앴습니다. 판 크기와 목표 별로 범위를 정하고(6×6 3~6, 8×8 3~7, 어려우면 위로, 쉬우면 상한만 아래로) 정답마다 조금 흔듭니다. 하한은 3 미만으로 내리지 않습니다(2칸 구역끼리 다시 짝지어져 유일해가 깨짐). 작은 판은 구역이 넷 이상 나오도록 누릅니다.
- 옛 공유 링크의 `min`·`max`는 그대로 읽습니다. CLI `--min/--max`는 주면 우선.

### 7. 표식 전용 퍼즐 (쌍둥이·닮지 않은 둘·넓이 구분만)
- 원작 스크린샷(4×4, 굵은 납선 5개, δ 둘, Ⅱ 하나)을 엔진에 넣으면 해가 하나입니다. 유일성은 "구역이 작아 어떤 쪼개기도 표식을 깨뜨림"에서 나옵니다.
- 그래서 벽 모드에서 넓이 규칙이 없으면 구역을 1~3칸(넓이 구분은 1~4, 가끔 +1)으로 만듭니다(`markerOnly`). 측정: 비정형 판에서 "벽 전부 + 표식 전부"가 정답을 고정하는 비율이 1~3칸이면 절반, 1~5칸이면 0.
- 벽 모드의 최소화 순서: 어려운 목표(하한 3★ 이상, 표식 전용 제외)는 벽을 먼저(정답당 한 번), 그 외에는 단서를 먼저 지우고 벽을 다듬습니다. 먼저 지우는 쪽이 사라지고 남는 쪽이 퍼즐을 떠받칩니다. 벽은 쉬운 정보, 표식·기호는 어려운 정보.
- 웹의 "단독 불가" 안내(`warnCannotOutline`)는 제거했습니다.

### 8. 넓이 구분·장미창 단독 모드 (`partition.ts`, `generate.ts`)
- `growOnce`가 넓이 구분일 때 이웃과 다른 크기를 고릅니다. 7×7에서도 즉시 나옵니다.
- 장미창 단독 모드: 끝 칸이 기호 수보다 많은 구역은 분할 단계에서 다시 키웁니다(`maxTips`). 하한은 기호 수 k. 장미창 단독도 벽 모드를 거칩니다(직사각 판의 2×2 도미노 대각 기호 문제를 벽 하나가 풉니다).

### 9. UI 세 가지 (`src/web/board.ts`, `src/web/model.ts`)
- **칠한 칸 두 번 탭**: 그 구역 둘레에 경계선 일괄. 이미 다 둘러져 있으면 제거. 첫 탭의 지우기는 되돌리고 진행합니다(`lastErase`, 320ms).
- **길게 누르기**: 칠한 칸이면 그 구역, 빈 칸이면 벽·색칠에 막히지 않고 이어진 빈 칸의 수(`PlayerState.areaOf`).
- **오류 빗금**: 단서가 있는 칸은 가장자리 띠로만 그려 숫자·기호가 보입니다.
- 헤드리스 크롬에서 실제로 눌러 확인했습니다(`scripts/ui-drive.mjs`).

## 검증 스크립트

- `node scripts/sweep.ts [4,7,9]` — 웹 폼의 모든 규칙 조합 × 크기 × 1★/7★ × 직사각/비정형. 30~40분. `grep -E "NONE|missed"`. 회귀 판별은 옛 커밋을 `git archive <commit> | tar -x -C /tmp/old`로 풀어 같은 스크립트를 돌려 비교.
- `node scripts/ui-drive.mjs <출력폴더>` — 빌드된 앱을 헤드리스 크롬(CDP)으로 조작해 빗금·두 번 탭·길게 누르기 스크린샷. 사용법은 파일 머리에.

## 2026-09-22 후속 작업 (두 번째 세션)

### 10. 전체 sweep 완주와 회귀 판별
- `scripts/sweep.ts`를 이번 코드와 옛 커밋 `c6a88b9`에서 끝까지 돌렸습니다(각 228회, 로그는 `docs/sweep/`: 11번 수정 전 `sweep-before.log`, 옛 커밋 `sweep-c6a88b9.log`, 수정 후 해당 조합만 `sweep-after-subset.log`). NONE은 옛 코드 92개 → 이번 코드 21개.
- 옛 코드에서는 되고 이번 코드에서 안 되던 것은 둘뿐이었고(9×9 직사각 넓이의 수 1★, 9×9 직사각 넓이의 수+장미창 기호 4개 7★), 아래 11번으로 고쳤습니다.
- 남은 NONE(둘 다 안 되거나 옛 코드에 없던 조합): 직사각 도형 창고 단독(7×7·9×9, 원래 불가), 9×9 넓이 구분 단독·장미창+넓이 구분·범위+넓이 구분(모두 `no-partition`: 이웃 크기가 모두 다른 분할을 200회 안에 못 만듦. 7×7까지는 됨. 고치려면 분할 후 같은 크기 이웃 쌍 사이에서 칸 하나를 옮기는 보수 단계를 넣으면 될 듯).

### 11. 직사각 + 경계선 없음 + 넓이의 수(+장미창) 유일해 (`clues.ts`, `generate.ts`)
- 원인은 장미창이 아니라 **같은 넓이의 이웃 구역**이었습니다. 숫자만으로는 같은 넓이의 두 이웃 구역이 칸을 맞바꿔도 숫자가 하나도 안 바뀌어, 9×9 직사각 판은 넓이의 수 단독으로도 전체 단서가 유일해인 정답이 20개 중 1개뿐입니다. 장미창은 기호 칸에서 숫자를 빼서 이를 더 악화시켰습니다.
- 고친 것 둘: (a) 다른 규칙이 구역을 그릴 때 장미창 기호를 끝 칸이 아니라 **안쪽 칸**(다른 구역과 닿은 변이 가장 적은 칸)에 둡니다(`roseSymbolCells`의 비강제 분기). (b) 넓이의 수가 구역을 그리는 조합(도형 창고·폴리오미노 없음)에서 유일해 실패가 두 번 이어지면 이웃 넓이가 모두 다른 분할(`sizeSeparation` 분할 옵션)로 바꿉니다(`distinctSizes`). 분할이 안 되면 상한을 1~2 넓힙니다.
- 측정(전체 단서 유일해 비율, 20개 정답): 7×7 k=2 끝 칸 1/20 → 안쪽 3/20 → 안쪽+크기 구분 18/18. 9×9 k=2 1/20 → 2/20 → 13/13. k=4는 크기 구분과 함께 절반 정도.
- 결과: 9×9 직사각 기호 4개 7★가 실패에서 27초, 8×8 기호 4개 133초 실패에서 7~9초. 기호 4개 1★ 요청은 되긴 하지만 9×9에서 160초(전체 단서로도 1★이 안 되어 상한을 올린 뒤 정답마다 한 번씩만 최소화).
- "기호 셀에 숫자를 함께 허용"(HANDOFF 옛 2번 안)은 측정상 안쪽 배치와 차이가 없어 UI를 바꾸지 않았습니다.
- `GenerateOptions.partition`: 정답 분할을 직접 넣는 훅(실험·테스트용)을 추가했습니다.

### 12. 표식 전용 6~7★ (`partition.ts`의 `pairPartition`)
- 한 쌍의 큰 쌍둥이 구역(5~9칸)만으로는 4~5★에 그쳤습니다. 효과가 있던 것은 **3~4칸 합동 구역 쌍을 여러 개** 서로 붙여 놓고 나머지를 1~2칸으로 자르는 구조입니다(`pairPartition`).
- 조건: 벽 모드 + 쌍둥이 규칙 포함 + 넓이 구분 없음 + 크기 미지정 + 목표 하한 6★ 이상(`twinPairs`). 쌍 수는 활성 칸의 3/4를 3.5칸 쌍으로 덮는 만큼 올림(6×6 4, 8×8 6). 8×8에 7쌍은 배치가 자주 실패합니다.
- 측정(시도 200회, 6~7★ 요청, 비정형+벽): 6×6 시드 12개 적중 1→3, 총 68→39초. 8×8 시드 6개 2→4, 171→46초. 개선은 두세 배지만 6×6은 여전히 대부분 5★ 미달입니다. 처음 6개 시드로 5/6이 나왔던 것은 우연이었고(12개로 늘리자 3/12), 첫 구현에는 두 번째 복사본이 첫 복사본과 겹치는 버그가 있어 고쳤습니다.
- 실패 원인 분포: 시도의 절반 이상이 `no-partition`(쌍 배치 실패, 싸다), 나머지는 5★ 미달. 더 올리려면 5★에서 멈추는 구조를 봐야 합니다(예: 쌍 사이에 닮지 않은 둘 표식으로 이어지는 사슬, 3~5칸 쌍은 효과 없었음).
- 쌍둥이 단독(`gemini`만)은 별점과 무관하게 6×6에서도 거의 생성되지 않습니다(`not-unique`: 쌍둥이 표식만으로는 작은 구역의 쪼개기를 막지 못함). 이전부터 그랬고 sweep 조합에는 없습니다.

## 확인된 한계 (2026-09-22 두 번째 세션 기준)

| 조합 | 상태 |
|---|---|
| 직사각 판의 도형 창고 단독 | 원래부터 불가(2×2 창고 외 타일링 비유일). 비정형 창 필요 |
| 쌍둥이 + 넓이 구분 | 논리 모순. 앱이 즉시 안내 |
| 쌍둥이 단독 | 거의 생성 불가(표식만으로 쪼개기를 못 막음) |
| 표식 전용 6~7★ | 6×6 적중 1/4 정도, 8×8 2/3. 미달이면 5★ 반환(12번 항목) |
| 폴리오미노 단독 높은 별 | 6×6 천장 2~3★. 9×9 7★ 요청은 몇 분 |
| 직사각 + 경계선 없음 + 넓이의 수 + 장미창 | 됨(11번). 기호 4개 1★ 요청은 9×9에서 160초 |
| 9×9 넓이 구분 단독·장미창+넓이 구분·범위+넓이 구분 | `no-partition`(10번). 7×7까지는 됨 |
| 4×4처럼 작은 판의 7★ | 대부분 미달(정상: 판이 작아 어려워질 수 없음) |

## 다음에 할 일

1. 9×9 넓이 구분 분할: 같은 크기 이웃 쌍 사이에서 칸 하나를 옮기는 보수 단계(`growPartition`)로 `no-partition`을 없앨 수 있을 듯.
2. 표식 전용 6~7★ 적중률: 12번 항목의 실패 분포 참고.
3. 기호 4개 + 넓이의 수의 1★ 요청이 큰 판에서 느린 것(상한을 올린 뒤 정답마다 한 번만 최소화하는데 정답 준비가 비쌈).
4. `docs/CONTROLS.md`의 "경계 길게 누르기 → X 마크"는 하지 않기로 했습니다.

## 측정 요약 (6×6, 넓이의 수+쌍둥이+닮지 않은 둘, 비정형+고정 경계선, seed 6개)

| 요청 | 적중 | 시간 |
|---|---|---|
| 1~2★ | 6/6 | 0.1 s |
| 3~4★ | 6/6 | 0.2 s |
| 5~6★ | 6/6 | 0.3 s |
| 7★ | 6/6 | 1.5 s |

8×8은 각각 0.6 / 1.2 / 2.9 / 6.8초.

## 2026-09-22 후속 작업 (세 번째 세션): A급 규칙 검증과 구현

`docs/A_RULES.md`(GIMMICKS.md의 A급 여섯 규칙을 원작 자료로 검증한 문서)를 쓰고, 그 "다음에 할 것"을 같은 세션에 구현했습니다. 검증 요지: 난이도 수치는 모두 맞고, **Solitude의 정의만 틀렸으며**(원작은 어떤 규칙의 단서 타일이든 구역마다 정확히 하나. 단독·장미창과 함께 나오지 않음), Difference는 규칙 카드에 차이 0 표식이 있습니다.

### 13. 새 규칙 다섯 (`src/engine/rules/solitude.ts`, `boxy.ts`, `compare.ts`)

| 규칙 | clue | 전파 | 생성 |
|---|---|---|---|
| `solitude` | 전역. 기호 = 넓이의 수·폴리오미노·장미 기호 셀 | `RoseRule`을 k=1, 기호=단서 셀로 재사용(`one-symbol` 벽 기법) | 구역마다 단서 셀 하나(종류는 켜진 규칙 중 무작위), 전부 `required`. 단서가 구역당 하나라 윤곽을 못 잡으므로 **벽 모드**로 감 |
| `boxy` | 전역 | 경계 상자 채우기(`boxy-fill` 1등급), 상자 넓이 하한, 후보 직사각형 열거(≤60개, `shape-place` 3등급), `mergeConflict`(합친 상자에 벽·구멍·넓이 초과), `placementConflict` | `rectangleBank(lo,hi)`로 `tilePartition`. 도형 창고와 함께면 카탈로그를 직사각형으로 거름 |
| `nonBoxy` | 전역 | 하한 3(`regionSizes`), 직사각형 성분은 자라야 함(출구 하나면 `forced-exit`), `placementConflict` | `growPartition`의 `noRectangles`(다시 키우기 + 거부) |
| `inequality` | 엣지 표식 `larger: 'a'|'b'` | 벽 + 구간 좁히기 `A.lo ≥ B.lo+1`, `B.hi ≤ A.hi−1` | 넓이가 다른 이웃 쌍마다 표식 후보(쌍당 2개) |
| `difference` | 엣지 표식 `value ≥ 0` | 벽 + `[B.lo−N, B.hi+N]`, 한쪽이 확정되면 `{x−N, x+N}` 중 구간에 드는 것. 가운데는 `placementConflict`로만 배제 | 이웃 쌍마다 표식 후보. 0 포함 |

- 조합 제한은 `ruleConflict()`(generate.ts) 한 곳에: 쌍둥이+넓이 구분, 장미창+유일, 사각틀+사각틀 금지, 유일에 단서 규칙 없음, **사각틀에 넓이 규칙 없음**(직사각형은 언제나 둘로 잘리므로 표식만으로는 유일해 불가). 웹 폼은 이 결과로 경고하고 CLI는 오류.
- 웹: 폼 체크박스 5개, 목표 대화상자·칩 문구(ko/en), 오류 빗금(유일: 기호 없는 벽 구역·기호 둘인 색 구역, 사각틀/금지: 확정 구역 모양, 불평등·차이: 확정 이웃 쌍), 힌트 문구. 불평등 표식은 원작 카드처럼 **벌어진 쪽이 큰 구역**인 꺾쇠, 차이는 원 안의 숫자.
- 기존 `rose k=1`은 이름만 "기호 한 종류 장미창"으로(`--rose 1`). 원작 Act I에도 19개 있는 형태라 그대로 둡니다.

### 14. 나눗셈 추론 `pocket-count` (`core.ts`)

원작 Precision 풀이의 62/66 보드가 쓰는 "이 경계선을 그으면 11칸이 잘리는데 11은 4의 배수가 아니다"를 넣었습니다. 매 전파 라운드에 비벽 엣지 그래프의 다리(bridge, 반복 Tarjan)를 구하고, 미정 다리를 벽으로 두면 생기는 두 주머니의 칸 수가 허용 넓이의 합으로 표현 불가능하면 이음. 허용 넓이는 규칙의 `regionSizes()` 훅을 교집합한 것(Range 구간, Shape Bank 크기 집합, Non-Boxy 하한 3). 이미 벽으로 닫힌 영역의 칸 수가 표현 불가능하면 모순. Precision(허용 넓이 하나)은 1등급, 그 외 2등급.

- 효과: 6×6 비정형+벽 Precision 4의 5~7★ 요청이 시드 4개에서 옛 코드 `7 5 5 6` → `4m 3m 4m 5`. A_RULES가 예측한 대로 Precision 별점이 내려갑니다(원작이 1등급으로 보는 단계를 가정으로 세지 않게 됨). 다른 조합(넓이의 수+쌍둥이, 범위, 도형 창고, 넓이의 수+범위)은 시드 4개씩 모두 같은 별점.
- 비용: 라운드당 O(칸+엣지). 8×8 넓이의 수 7★ 생성 2.0초로 이전과 같음.

### 15. 측정 (6×6·8×8, seed 1, 시도 40, 1★과 7★ 요청, 직사각/비정형+벽) — `docs/sweep/sweep-a-rules.log`

- 1★ 요청은 모든 조합에서 즉시(≤1초) 나옵니다.
- 7★: 넓이의 수와 묶인 불평등·차이·사각틀 금지는 6×6에서도 7★이 나옵니다(0.4~1.8초). 유일+넓이의 수는 6×6에서 6★, 8×8에서 7★(5.6초). 사각틀은 6×6 직사각에서 5★이 천장이고 넓이의 수와 함께 10초. 사각틀+장미창은 비정형에서 실패(직사각형은 끝 칸이 4개라 장미 기호 강제 배치가 안 됨, `roseForced`).
- 유일+폴리오미노는 2★ 천장(폴리오미노 단독의 천장과 같음).

### 확인된 한계(추가)

| 조합 | 상태 |
|---|---|
| 사각틀 단독·표식만 | 불가(`boxy-needs-size`). 직사각형은 둘로 잘림 |
| 사각틀 + 장미창 (비정형) | `no-clues`: 2×2·2×3 직사각형의 끝 칸 4개 > 기호 수 |
| 사각틀 7★ | 6×6에서 5★ 천장, 8×8 직사각 넓이의 수와 함께 7★(7.5초) |
| 유일 + 폴리오미노만 | 2★ 천장(6×6), 8×8 5★ |
| 사각틀 금지 단독 | 벽 모드. 큰 구역은 비직사각형 구역이 둘로 잘려 `not-unique`가 잦아, 열여섯 번 연속 실패하면 구역을 3~5칸으로 줄임(5칸 이하 비직사각형은 3칸 이상 둘로 못 자름). 6×6 7★ 요청은 시드에 따라 2~5★, 8×8은 2~3★ |
| 불평등 + 넓이 구분 (비정형 8×8) | `no-partition`: 넓이 구분 분할의 기존 한계(10번) |

## 2026-09-22 후속 작업 (네 번째 세션): 원작 어려운 퍼즐 구조에 맞추기

`docs/HARD_PUZZLES.md`에 원작 Act I 난이도 5~7 판 51개의 구조(정답 분할을 풀이 단계에서 복원)와 그에 맞춘 변경을 정리했습니다. 요지: 어려운 판은 구역이 크지 않고 **판이 큽니다**(평균 84칸, 구역 19개, 구역 넓이 5.4칸, 벽 14%, 단서 밀도는 쉬운 판과 같음). 우리 생성기는 9×9 이상에서 대부분 실패하고 있었습니다.

### 16. 생성기 변경 (`generate.ts`, `partition.ts`, `mask.ts`)
- `exactPartition`: Precision(min=max=N)은 N≤7이면 그 크기의 모든 자유 폴리오미노(`allPolyominoes`)로 타일링, 그 이상은 성장 400회. 성장식은 큰 판을 정확히 N칸씩 못 채웠습니다.
- `punchToMultiple`: 칸 수가 N(또는 창고 크기의 최대공약수)의 배수가 아니면 바깥에서 칸을 파내 맞춥니다. 직사각 모드에서도 적용되므로 9×9에 Precision 4를 요청하면 구멍 하나가 생깁니다.
- `tileWithHoles`: 비정형 창고 판에서 타일링이 실패하면 창고로 채우다 못 덮는 칸을 구멍으로(20%까지, 연결만 검사). 원작 큰 창고 판의 구멍이 타일 모양인 것과 같은 원리.
- 쌍둥이 요청 시 `pairPartition`으로 합동 쌍 2~3개를 먼저 심음(9×9에서 `no-clues` 12/12 → 7★).
- `bigRose`: 장미창 기호 4종 이상 단독이면 구역 2k~5k칸, 기호는 아무 칸, 벽 모드. 12×12 기호 5종은 구역 6개(14~23칸)로 원작 1434와 같은 구조.
- `mingle` 규칙 추가(`MingleRule`, `distinctNeighbours` 분할·타일링 검사, 웹·i18n·오류 표시·sweep). 쌍둥이와는 `gemini-mingle` 충돌.
- 큰 판에서 숫자 후보를 구역당 3개로 줄여 보았으나 전체 단서 집합의 유일해가 깨져 더 느려져 되돌렸습니다.
- 웹 폼 크기 상한 10 → 12.

### 측정 (seed 1, 시도 12, 6~7★): HARD_PUZZLES.md 4절. 9×9 Precision 5 비정형 7★ 4.6초, 11×11 9.6초, 9×9 넓이의 수+쌍둥이 7★ 21초, 11×11 Precision 4+표식 7★ 90초, Mingle+창고 9×9 7★ 1.7초.

### 확인된 한계(추가)
| 조합 | 상태 |
|---|---|
| 도형 창고 단독 9×9 이상 | 유일해 실패 잦음(원작 어려운 판의 최다 유형). 보수 횟수 확대나 벽 허용이 다음 과제 |
| Mingle 단독 | 6×6에서만 가끔. 1~4칸 구역의 무작위 분할이 같은 모양 이웃을 너무 많이 만듦 |
| 11×11 표식·숫자 조합 | 1~2분 |

## 2026-09-22 후속 작업 (다섯 번째 세션): 전 창 규칙 조합 전수 분석과 여섯 규칙 추가

`docs/MIXED_RULES.md`. 팬 사이트에 Act II·III 보드는 아직 없어(soon), camzillasmom 창별 풀이 페이지 17개(351개 퍼즐의 "Rules to follow" 목록과 레벨)를 파싱해 Act I 보드 데이터와 합쳤습니다(663개 퍼즐, 114개 조합). 파서는 `\nID: 12345\n`로 퍼즐을 나누고 규칙 줄을 읽는데, 마지막 줄에 개행이 없어 각 퍼즐의 마지막 규칙이 빠지는 버그가 있었습니다(사각틀 단독 16개로 보였던 것이 실은 전부 혼합). 고친 뒤 집계했습니다.

### 17. 새 규칙 여섯 (`rules/shapes.ts`의 Match·Mismatch, `rules/topo.ts`의 Palisade·Bricky·Loopy·Watchtower)
- 타입·검증·렌더·i18n·웹 폼·오류 표시·힌트 문구·테스트까지. 규칙 수 20(원작 24 중 Compass·Min/Max 개별·(Mingle 포함)). `grid.ts`에 `neighbourInDirection`, `vertexCells`, `vertexEdges`.
- **Mismatch 세기 추론**(`mismatch-count`, 2등급): 벽으로 갈라진 주머니마다, 밖에서 m칸 이하 모양이 전부 쓰였으면 2m+1칸 이하 주머니의 안쪽 엣지는 이음. 고정 모양의 제외도 다른 주머니에만. 같은 주머니 안 성분에 제외를 적용하면 정답이 거부됩니다(처음 실수).
- 분할: `mismatchPartition`(작은 모양 전부 심기), `loopyPartition`(섬과 바다), `noCross`(Bricky), `distinctShapes`(Mismatch; 성장·타일링 둘 다), `punchToAtMost`(불일치+정밀).
- Boxy는 불일치·울타리·감시탑과도 허용. 장미창+사각틀·장미창+불일치는 기호를 아무 칸에(직사각형·세기가 자름을 막음).
- 웹 폼 체크박스 20개. 감시탑은 꼭짓점 위 검은 원에 숫자, 울타리는 칸 안 작은 정사각형의 굵은 변.

### 측정: `docs/sweep/sweep-mixed-7x7.log` (원작 조합 83개 × 1~2★/5~7★, 7×7 비정형+벽). 실패 원인과 조치는 MIXED_RULES.md 5절.

### 확인된 한계(추가)
| 조합 | 상태 |
|---|---|
| 울타리 계열 | 1★ 천장(타일이 변을 바로 정해 우리 솔버가 전부 1등급으로 봄). 원작은 5~6 |
| 불일치 + 정밀한 넓이 N=4 | 20칸 판 필요. 7×7 요청은 `no-partition` |
| 사각틀 + 불일치 직사각 6×6 | `not-logical`/`not-unique`. 7×7 비정형은 2~3★ |
| Compass, 최소·최대 개별 | 미구현 |
