{
  "name": "pane-by-pane",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Puzzle engine (generator + solver) for a Glimmith-style grid partition game",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test \"tests/**/*.test.ts\"",
    "pbp": "node src/cli.ts",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dev:lan": "vite --host",
    "preview:lan": "vite preview --host"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vite": "^8.3.0"
  }
}
